# Python
.venv/
__pycache__/
*.pyc

# Data (downloaded separately via hf CLI)
hf_cache/
*.parquet
*.jsonl
*.png

# Jupyter
.ipynb_checkpoints/

# uv
# (uv.lock is committed — it pins the full dependency tree)

# Build artifacts
*.egg-info/
build/
dist/

# MindRL submission — keep submission artifacts out of git
scores.json
validation_report.json
.env
.env.*
secrets.env
*.log
artifacts/*
!artifacts/README.md
.DS_Store

# Local-only explanation files (not part of the submission contract)
AGENT_EXPLAINED.md
SUBMISSION.md